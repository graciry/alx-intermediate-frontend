# 0-installation-script

# Step 1: Install Node.js from https://nodejs.org/en/download
# Step 2: Open terminal (Command Prompt or Git Bash)
# Step 3: Install Sass globally with:
npm install -g sass

# Step 4: Confirm Sass is installed:
sass --version
